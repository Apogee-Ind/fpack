tracking playtime:
run every second
check if afk
if not afk, add 1 to playtime
if not afk, add 1 to time since death
if actual time since death is less than 40 ticks ago, set time since death to 0
convert both from seconds to hours

tag for each role?

need function to reset all players (including offline players) at the start of every month
before reset, value of each player's role needs to be read and used to update their role if necessary

